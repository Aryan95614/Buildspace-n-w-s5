import 'package:flutter/material.dart';

class EndpointsConfig extends StatefulWidget {
  const EndpointsConfig({super.key});

  @override
  State<EndpointsConfig> createState() => _EndpointsConfigState();
}

class _EndpointsConfigState extends State<EndpointsConfig> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [Text("Adding Endpoints Place")],
      ),
    );
  }
}
